from main_bot import bot_main
import asyncio


if __name__ == "__main__":
    asyncio.run(bot_main())
    #from alerts.alerts import send_success_record_to_user
    #asyncio.run(send_success_record_to_user(485083720, "каки"))
    #asyncio.run(main_record())
